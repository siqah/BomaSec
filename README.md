# BomaSec
This tech prevents data leaks in multi-tenant applications by implementing layered, fail-safe isolation, utilizing PostgreSQL Row-Level Security and unique OpenSearch indices for each tenant.This approach ensures security by separating data at both the database and storage levels, mathematically preventing leaks even if application code fails
